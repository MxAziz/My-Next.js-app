import Counter from '@/components/Counter';
import React from 'react';

const CounterPage = () => {
    return (
        <div>
            <Counter></Counter>
        </div>
    );
};

export default CounterPage;